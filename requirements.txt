pygame==2.1.3
# mediapipe is required by Main_Game_Scene/face_capture.py
# Note: mediapipe may not provide prebuilt wheels for very new Python versions (e.g. 3.13).
# If `pip install mediapipe` fails, install and use Python 3.11 (recommended) or see troubleshooting below.
mediapipe
