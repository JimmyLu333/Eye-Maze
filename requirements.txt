## Pin minimal runtime dependencies for Eye-Maze
# Recorded from the active environment on 2025-11-10
opencv-python==4.12.0.88
mediapipe==0.10.14
pygame==2.6.1

# Notes:
# - mediapipe wheels may not be available for very new Python versions. If
#   `pip install mediapipe` fails, consider using Python 3.11 or consult
#   the mediapipe installation docs.
pygame==2.1.3
numpy
